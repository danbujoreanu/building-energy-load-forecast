# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.mypy_cache/
.ruff_cache/

# ── Virtual environments ──────────────────────────────────────────────────────
.env
.venv
env/
venv/
ENV/

# ── Jupyter ───────────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb_checkpoints

# ── Large processed data files ───────────────────────────────────────────────
# Raw building files are kept; generated/merged files are not
data/processed/
data/raw/

# ── Model weights (large binaries) ───────────────────────────────────────────
outputs/models/
*.h5
*.pkl
*.pt
*.pth
*.ckpt
lightning_logs/

# ── Generated outputs ─────────────────────────────────────────────────────────
outputs/figures/
outputs/logs/
# outputs/results/ is committed (small CSVs with metrics)

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes

# ── IDEs ──────────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo

# ── Secrets ───────────────────────────────────────────────────────────────────
.env
*.env
secrets.yaml

# ── Commercial / private — NEVER include in public academic repo ───────────────
# These files live in this private repo only.
# When creating the public academic snapshot, these are excluded by
# scripts/create_public_snapshot.sh
#
# deployment/          ← commercial product code (ControlEngine, connectors, API)
# docs/COMMERCIAL_ANALYSIS.md  ← product roadmap, competitive analysis, go-to-market
#
# To create a public academic snapshot:
#   bash scripts/create_public_snapshot.sh
outputs/predictions/
# One-off utility scripts (machine-specific paths)
extract_pdf.py

# Claude agent config (local only)
.claude/

# Private operational, commercial & personal files — NEVER commit
CLAUDE.md
CAREER_CONTEXT.md
docs/ops/
docs/commercial/
knowledge/procedural/EDDI_API.md
.env
.env*
*.env

# Career intelligence — personal profiles and job research (NEVER in git)
# These are private documents: other people's profiles, interview notes, job specs
intel/docs/career/

# Personal meter data — NEVER commit (contains home energy consumption patterns)
data/ESB\ Smart\ Meter\ Data/
data/ESB*
HDF_calckWh*.csv
HDF_kW*.csv

# ChromaDB vector store (large binaries, machine-specific)
data/chromadb/

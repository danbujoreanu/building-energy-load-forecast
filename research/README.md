# research/

MSc AI thesis artefacts (NCI, AICS 2025). Nothing here runs in production.

## Structure

| Directory | Contents |
|-----------|----------|
| `scripts/` | 24 Python scripts — thesis experiments, one-off analyses, paper figures |
| `checkpoints/` | TFT PyTorch Lightning checkpoints (gitignored — large binaries) |
| `lightning_logs/` | TFT training logs, versions 0–39 (gitignored) |

See `scripts/README.md` for per-script descriptions.
